/* Program to find the SADDLE POINT in the given matrix */
/*
#include <stdio.h>
#include <stdlib.h>

//void main()
{
	int row,cols,saddlePt,i,j;
	int *matrix;
	matrix=(int*)malloc(sizeof(int)*200);

	printf("\nEnter no.of rows:");
	scanf("%d",rows);

	printf("\nEnter no.of columns:");
	scanf("%d",cols);

    realloc(matrix,sizeof(int)*rows*cols);

*/